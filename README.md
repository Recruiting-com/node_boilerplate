# node_boilerplate

### Setting up development

install nvm:

use `4` or later

`npm install`

install foreman:

`sudo gem install foreman`

run:

`make dev`


### Testing

`make test`


### Production

`make deploy`
